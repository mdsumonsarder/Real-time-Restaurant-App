package verzac.tastebuddy.helper;

import com.google.firebase.database.FirebaseDatabase;

import verzac.tastebuddy.models.MenuItem;
import verzac.tastebuddy.models.Order;
import verzac.tastebuddy.models.Restaurant;

/**
 * Created by verzac on 5/18/2017.
 */

public class TestDataInit {
    public TestDataInit(){
        /*
        Restaurant maccas = new Restaurant("Macca's", "42 Princes Hwy");
        MenuItem hamburger = new MenuItem("Hamburger", "This is a hamburger.", 8);
        MenuItem chickenstrips = new MenuItem("Chicken Strips", "This is a chicken strip.", 13);
        Order order1 = new Order(maccas, "Monash Caulfield", "TEST_USER_ID", , );

        Restaurant kfc = new Restaurant("KFC", "42 Princes Hwy");
        MenuItem chickenwing = new MenuItem("Chicken Wing", "This is not a hamburger.", 8);
        MenuItem nugget = new MenuItem("Chicken Nugget", "This is not a chicken strip.", 13);
        Order order2 = new Order(kfc, "23 College Walk", "TEST_USER_ID", , );

        order1.addItem(hamburger);
        order1.addItem(chickenstrips);
        order2.addItem(chickenwing);
        order2.addItem(nugget);
        order2.addItem(chickenstrips);

        FirebaseDatabase.getInstance().getReference(Order.ORDER_KEY).push().setValue(order1);
        FirebaseDatabase.getInstance().getReference(Order.ORDER_KEY).push().setValue(order2);
        FirebaseDatabase.getInstance().getReference(Order.ORDER_KEY).push().setValue(order2);
        FirebaseDatabase.getInstance().getReference(Order.ORDER_KEY).push().setValue(order1);
        FirebaseDatabase.getInstance().getReference(Order.ORDER_KEY).push().setValue(order2);
        FirebaseDatabase.getInstance().getReference(Order.ORDER_KEY).push().setValue(order1);
        FirebaseDatabase.getInstance().getReference(Order.ORDER_KEY).push().setValue(order1);
        FirebaseDatabase.getInstance().getReference(Order.ORDER_KEY).push().setValue(order2);
        FirebaseDatabase.getInstance().getReference(Order.ORDER_KEY).push().setValue(order1);
        FirebaseDatabase.getInstance().getReference(Order.ORDER_KEY).push().setValue(order2);
        FirebaseDatabase.getInstance().getReference(Order.ORDER_KEY).push().setValue(order2);
        */
    }
}
